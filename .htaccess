<IfModule mod_rewrite.c>
	
	RewriteEngine on
	RewriteCond $1 !^(index\.php|favicon\.ico|sites)
	RewriteRule ^(.*)$ index.php/$1 [L]

</IfModule> 

# Habilitar o PHP 5.3
#AddType application/x-httpd-php53 .php